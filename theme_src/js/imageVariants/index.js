import { ImageVariants } from './ImageVariants'

/**
 * Initialize the image variants Listeners.
 * @returns {ImageVariants} The image variants instance.
 */
export function initImageVariants() {
    return new ImageVariants();
}
